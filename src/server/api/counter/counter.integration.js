'use strict';

var app = require('../..');
var request = require('supertest');

var newBoard;

describe('CounterGroup API:', function () {

    describe('GET /api/countergroups', function () {
    //     var counterGroups;

    //     // before each is not needed here. this is the actual test
    //     beforeEach(function (done) {
    //         request(app)
    //             .get('/api/countergroups')
    //             .expect(200)
    //             .expect('Content-Type', /json/)
    //             .end((err, res) => {
    //                 if (err) {
    //                     throw new Error(err);
    //                 }
    //                 counterGroups = res.body;
    //                 done();
    //             });
    //     });

    //     it('should respond with JSON array', function () {
    //         counterGroups.should.be.instanceOf(Array);
    //     });

    });   
});
